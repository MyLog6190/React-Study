import React from "react";
import ReactDOM from "react-dom/client";
import App from "./_05_create_react_app/App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
